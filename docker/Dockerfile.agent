FROM python:3.11-slim

WORKDIR /app

# context já é ./agents — não repetir o prefixo nos caminhos
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "collector.py"]
